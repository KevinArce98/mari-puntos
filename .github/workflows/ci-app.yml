name: CI - Mobile App

on:
  pull_request:
    paths:
      - 'mari-puntos-app/**'
      - '.github/workflows/ci-app.yml'

defaults:
  run:
    working-directory: mari-puntos-app

jobs:
  lint-and-typecheck:
    name: Lint & TypeCheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: mari-puntos-app/node_modules
          key: ${{ runner.os }}-npm-app-${{ hashFiles('mari-puntos-app/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-app-

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: TypeCheck
        run: npx tsc --noEmit
